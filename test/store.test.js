import {describe,it,expect} from 'vitest';
import {missions,rewards,initialAircon,weatherLocation,getHourlyWeather} from '../src/data.js';
describe('GreenON 기본 데이터',()=>{it('미션 보상과 시간이 유효하다',()=>{expect(missions[0].target_minutes).toBe(120);expect(missions[0].reward_points).toBe(300)});it('모든 상품 가격이 양수다',()=>expect(rewards.every(r=>r.points>0)).toBe(true));it('초기 에어컨은 미션 조건을 만족한다',()=>{expect(initialAircon.temperature).toBeGreaterThanOrEqual(26);expect(initialAircon.sensorOk).toBe(true)});it('광주 기온을 한 시간 간격으로 24개 제공한다',()=>{const weather=getHourlyWeather(new Date(2026,7,12,18));expect(weatherLocation).toBe('광주');expect(weather).toHaveLength(24);expect(weather[0].label).toBe('지금');expect(weather[1].hour).toBe(19);expect(weather[6].label).toBe('내일 00시')})});
